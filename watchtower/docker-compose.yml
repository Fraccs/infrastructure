version: '3'

services:
  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    command: --label-enable --interval 20
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/fraccs/.docker/config.json:/config.json
