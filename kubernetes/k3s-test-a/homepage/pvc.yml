---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-config
  namespace: homepage
spec:
  accessModes:
    - ReadWriteMany
  volumeName: homepage-config
  storageClassName: nfs
  resources:
    requests:
      storage: 50Mi
