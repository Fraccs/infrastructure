---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-data
  namespace: kavita
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-slow
  resources:
    requests:
      storage: 500Mi
