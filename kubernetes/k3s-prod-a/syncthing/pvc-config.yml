---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-config
  namespace: syncthing
spec:
  accessModes:
    - ReadWriteMany
  volumeName: syncthing-config
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
