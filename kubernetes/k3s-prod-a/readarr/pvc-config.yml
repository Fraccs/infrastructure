---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config
  namespace: torrenting
spec:
  accessModes:
    - ReadWriteMany
  volumeName: readarr-config
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
