---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: grafana
spec:
  accessModes:
    - ReadWriteMany
  volumeName: grafana-data
  storageClassName: nfs
  resources:
    requests:
      storage: 50Mi
