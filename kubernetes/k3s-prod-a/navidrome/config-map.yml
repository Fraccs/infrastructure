---
apiVersion: v1
kind: ConfigMap
metadata:
  name: navidrome
  namespace: navidrome
data:
  ND_PROMETHEUS_ENABLED: "true"
  ND_PROMETHEUS_METRICSPATH: "/metrics" # This instance isn't exposed to the internet so this is fine
