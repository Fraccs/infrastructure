---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-roms
  namespace: romm
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: romm-roms
  storageClassName: nfs
  resources:
    requests:
      storage: 50Gi
