---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-encrypted
  namespace: mosquitto
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-crypto-global
  resources:
    requests:
      storage: 256Mi
