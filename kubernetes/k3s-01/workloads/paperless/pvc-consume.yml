---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-consume
  namespace: paperless
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: paperless-consume
  storageClassName: nfs
  resources:
    requests:
      storage: 10Gi
