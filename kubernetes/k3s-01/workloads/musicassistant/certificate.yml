---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: musicassistant
  namespace: musicassistant
spec:
  secretName: musicassistant-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-clusterissuer-acme-letsencrypt-cloudflare
  dnsNames:
    - musicassistant.47fc5c.com
