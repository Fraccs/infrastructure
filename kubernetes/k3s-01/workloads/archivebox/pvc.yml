---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archivebox-data-encrypted
  namespace: archivebox
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-crypto-global
  resources:
    requests:
      storage: 1Gi
