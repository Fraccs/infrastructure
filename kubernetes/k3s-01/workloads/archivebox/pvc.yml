---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archivebox-data
  namespace: archivebox
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
