---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: endurain
  namespace: endurain
spec:
  secretName: endurain-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-clusterissuer-acme-letsencrypt-cloudflare
  dnsNames:
    - endurain.47fc5c.com
