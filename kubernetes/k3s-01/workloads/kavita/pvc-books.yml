---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-books
  namespace: kavita
spec:
  accessModes:
    - ReadOnlyMany
  volumeName: kavita-books
  storageClassName: nfs
  resources:
    requests:
      storage: 5Gi
