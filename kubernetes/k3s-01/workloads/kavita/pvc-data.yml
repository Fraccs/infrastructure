---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-data
  namespace: kavita
spec:
  accessModes:
    - ReadWriteMany
  volumeName: kavita-data
  storageClassName: nfs
  resources:
    requests:
      storage: 500Mi
