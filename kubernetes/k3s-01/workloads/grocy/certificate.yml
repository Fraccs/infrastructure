---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grocy
  namespace: grocy
spec:
  secretName: grocy-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-clusterissuer-acme-letsencrypt-cloudflare
  dnsNames:
    - grocy.47fc5c.com
