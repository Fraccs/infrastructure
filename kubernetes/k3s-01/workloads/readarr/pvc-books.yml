---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-books
  namespace: torrenting
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: readarr-books
  storageClassName: nfs
  resources:
    requests:
      storage: 5Gi
