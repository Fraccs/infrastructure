---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flower
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flower
  template:
    metadata:
      labels:
        app: flower
    spec:
      containers:
        - name: flower
          image: docker.io/mher/flower:2.1.0@sha256:02c4c468637b9c8df5e5104248fd085a1ac3f15347105784d76155198154a1ad
          command:
            - "celery"
          args:
            - "flower"
            - "--port=5555"
            - "--persistent=True"
          envFrom:
            - secretRef:
                name: flower
          env:
            - name: PORT
              value: "5555"
            - name: FLOWER_PORT
              value: "5555"
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
            limits:
              cpu: 100m
              memory: 256Mi
          ports:
            - name: http
              containerPort: 5555
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
