---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flower
  namespace: monitoring
spec:
  secretName: flower-certificate
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-clusterissuer-acme-letsencrypt-cloudflare
  dnsNames:
    - flower.47fc5c.com
